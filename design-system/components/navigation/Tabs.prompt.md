Peer views within one page — service categories, before/after.

```jsx
<Tabs value={tab} onChange={setTab} tabs={[{id:'intake',label:'Client intake',icon:'inbox'},{id:'billing',label:'Billing',icon:'file-text'}]} />
```

Active tab gets a 2px green underline (or a white pill). Labels are nouns, sentence case.
