On/off for something that takes effect immediately; use Checkbox inside forms that get submitted.

```jsx
<Switch label="Pause this workflow" description="Nothing runs until you switch it back on." checked={p} onChange={setP} />
```
