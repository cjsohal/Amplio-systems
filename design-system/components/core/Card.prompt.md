Every panel in the system. Default is white, 16px radius, soft warm shadow, no border.

```jsx
<Card variant="raised" padding="lg" interactive href="/services/onboarding">…</Card>
```

Don't stack shadows: a raised card inside a raised card should switch to `outlined` or `sage`. `brand` uses the hero gradient and is limited to one panel per page.
