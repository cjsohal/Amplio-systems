Small read-only status pill — plan tiers, workflow states, "New".

```jsx
<Badge tone="success" dot>Live</Badge>
<Badge tone="brand" icon="sparkles">New</Badge>
```

Sentence case, one or two words. Use `solid` sparingly — it competes with primary buttons.
