Circular icon-only control for toolbars, dialog dismissals and mobile nav toggles.

```jsx
<IconButton icon="x" label="Close" />
<IconButton icon="menu" label="Open menu" variant="outline" />
```

`md` (44px) is the minimum for touch. `label` is required — it becomes both `aria-label` and the tooltip.
