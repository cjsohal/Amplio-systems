Interactive or removable label — filter chips, chosen categories.

```jsx
<Tag selected onClick={toggle}>Client onboarding</Tag>
<Tag onRemove={() => remove(id)}>Invoicing</Tag>
```

Use Badge instead when the label is read-only status.
