A few words of clarification on hover or keyboard focus.

```jsx
<Tooltip label="Runs every weekday at 8am"><IconButton icon="clock" label="Schedule" /></Tooltip>
```

Fades only — no scale or slide. Keep to one short phrase; anything longer belongs in the page.
