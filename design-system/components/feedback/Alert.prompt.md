Inline, in-flow message. Not a toast — it stays until the state changes.

```jsx
<Alert tone="success" title="Thanks — that's booked">You'll get a calendar invite in the next few minutes.</Alert>
```

Write the title as the outcome in plain words. Never blame the reader in `danger` copy.
